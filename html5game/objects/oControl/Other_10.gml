/// @description Ideas for implementation

/// Ideas for implementation

	// Use timelines for... branching timelines
	// State of the world progresses between points on timeline
	// Each timeline is a projected scenario
	// Are timelines scrubbable?
	// Easiest way to import data into each scenario object?
	
// Time array
	// Array or list instead of GM timelines
	// May be easier to import data, edit
	// Probably easier to scrub thru time
	
// Map
	// Images of area
	// Start with static map, e.g. of Cape Cod
	// Could have layers with transparency, one layer for each topographic elevation
	// Set a layer's image_blend to dark blue when sea level is at that layer
	
// Data source
	// Could manually add data in
	// Will probably need to parse data and manually simplify it
	// Read from text file? 
	// What files can GM read from?
	// What files can be included with HTML5 version?

// Controls
	// Develop GUI art
	// Dial for climate variable, e.g. num of factories, num of trees, etc.
